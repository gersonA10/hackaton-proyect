node_modules/
.env
.env.local
*.log
npm-debug.log*
.DS_Store
dist/
coverage/

# caché de focos generada en runtime (el seed sí se versiona)
data/fires-cache.json
data/reports.json

# 🔑 credencial de Firebase Admin — NUNCA subir a git
serviceAccount.json
*firebase-adminsdk*.json
